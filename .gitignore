# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Side-quests (local-only, not committed)
docs/side-quests/

# cc-forge working files (local-only, not committed)
.context/

# Astro
.astro/

# Wrangler (local D1 state, local secrets)
.wrangler/
.dev.vars

*.code-workspace

# Playwright MCP artifacts
.playwright-mcp/
/*.png

# album-cover skill scratch output (staging area, not final storage)
docs/music/covers/

# Python bytecode (scripts/)
__pycache__/
*.pyc

# Spotify library working files (local-only, contain personal library data)
docs/music/liked-songs.json
docs/music/sort-plan.json
docs/music/undo-*.json

# Local env (CARTO basemap key)
.env
.env.local

# Handoff docs (ephemeral session artifacts)
docs/handoff/
